# TC-FUSION

Separated project files:
- index.html — page structure
- css/style.css — extracted CSS
- js/app.js — extracted JavaScript

Open index.html in a browser to run the prototype.
The existing CDN dependencies (Chart.js, Leaflet, Font Awesome, Google Fonts) remain external.
